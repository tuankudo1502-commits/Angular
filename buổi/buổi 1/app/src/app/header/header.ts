import { Component } from '@angular/core';

@Component({
  selector: 'app-header', //tên gọi component = file html 
  imports: [], //hàm, thư viện cần dùng 
  templateUrl: './header.html',
  styleUrl: './header.css',
})
export class Header {
  //khai báo thuộc tính, biên
  //khai báo hàm, phương thức
  

} //tên gốc của component = import trong file ts
